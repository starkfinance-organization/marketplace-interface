export enum CONTEXT_ACTIONS {
	SET_WALLET_CONNECTED,
}

export enum WALLET_TYPES {
	EVM = "evm",
	STARKNET = "starknet",
}

export enum WALLETS {
	ARGENT_X = "argentX",
	BRAAVOS = "braavos",
	OKX = "okx",
}